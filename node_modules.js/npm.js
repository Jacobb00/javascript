/**4. Global Paket Kurulumu

Bazı paketler sadece projede değil her yerden erişilebilir olmalıdır. Örneğin komut satırı araçları (CLI).
Örnek:

npm install -g http-server

Kurulduktan sonra terminalden her yerde çalıştırabilirsin:

http-server    elirli paketi güncelle:

npm update package-name

    Tüm paketleri güncelle:

npm update

    Eski paketleri kontrol et:

npm outdated

    NPM kendisini güncelle:

npm install -g npm@latest

❌ 6. Paket Silme

    Bir paketi kaldır:

npm uninstall package-name

    Global paketi kaldır:

npm uninstall -g package-name

    package.json’dan da silmek için:

npm uninstall --save package-name
--save ve --save-dev farkı:

    --save: uygulamanın çalışması için gerekli olan paketler

    --save-dev: sadece geliştirme aşamasında gerekli olanlar
    Örnek: */