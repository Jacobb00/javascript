/**{
    "name": "my-node-app",            // Projenin adı
    "version": "1.0.0",              // Proje versiyonu
    "description": "A simple Node.js app",  // Proje açıklaması
    "main": "index.js",              // Projenin ana dosyası proje çalıştırıldığında ilk bu çalışır
    "scripts": {                     // Komut satırından çalıştırılacak komutlar
      "start": "node index.js"
    },
    "author": "Your Name",           // Projenin yazarı
    "license": "ISC",                 // Lisans bilgisi
    "dependencies": {
        "express": "^4.18.2" //çalıştırılacak paketler
     devDependencies: {
        "nodemon": "^3.1.7" //sadece geliştirme aşamasında gerekli olanlar
        Sadece geliştirme aşamasında gereken paketler (test, lint, build araçları gibi).

Üretim ortamında yüklenmez.

 Versiyon Aralıkları

dependencies içindeki versiyonlar, npm’e paket güncelleme esnasında nasıl davranacağını söyler:

    "^4.17.21" → 4.x.x sürümleri arasında (5.0.0 hariç) güncellemeye izin verir.

    "~4.17.21" → Sadece patch güncellemelerini (4.17.x) kabul eder.

    "4.17.21" → Kesin olarak bu versiyonu kullanır.

    "latest" → Her zaman en son kararlı sürümü kullanır.

    "git+https://..." → Git deposundan doğrudan paket kullanımı.


    {
  "scripts": {
    "start": "node index.js",         // Uygulamayı başlatır
    "test": "echo \"Running tests...\" && exit 0", // Test komutu örneği
    "dev": "nodemon index.js"         // Geliştirme modunda çalıştırma
  }
}
Terminalde şu komutları kullanırsın:

    Herhangi bir script çalıştırmak için:

npm run <script-adı>

Örneğin, yukarıdaki dev scriptini çalıştırmak için:

npm run dev

start scripti özel bir scripttir. npm run start yerine sadece şu da yazabilirsin:

npm start

Aynı şekilde test scripti için:

npm test



{
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest",
    "build": "webpack --mode production",
    "lint": "eslint ."
  }
}
npm start → index.js dosyasını çalıştırır.

npm run dev → nodemon ile geliştirme modunda çalıştırır.

npm test → Jest testlerini çalıştırır.

npm run build → Webpack ile projeyi üretim modunda derler.

npm run lint → ESLint ile kodu denetler.
     }
    }
  }
  */